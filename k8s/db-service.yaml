apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: bytecart-db
spec:
  type: NodePort
  selector:
    app: bytecart-db
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30002
