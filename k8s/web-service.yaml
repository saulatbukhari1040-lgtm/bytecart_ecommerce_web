apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: bytecart-web
spec:
  type: NodePort
  selector:
    app: bytecart-web
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30001
